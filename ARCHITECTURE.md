# Architecture

## 第一版形態

本機可直接開啟的 HTML、CSS、JavaScript 原型，不依賴後端。先驗證素材輸入、步驟編輯、模板預覽與輸出體驗，再接 AI 與媒體渲染服務。

## 模組與資料流

1. `index.html`：三欄工作台，提供素材來源、步驟編輯與即時預覽。
2. `app.js`：示範步驟資料、拖曳排序、內容編輯、模板切換、匯出互動。
3. `styles.css`：企業 SOP 編輯器的視覺系統與列印樣式。
4. 未來 AI 層：將文字、公開網址、截圖與錄影整理成統一的 Step JSON。
5. 未來輸出層：把 Step JSON 送入 PDF、圖卡、漫畫與影片渲染器。

## 下一階段預留

- OCR 與畫面元素辨識。
- 可修正的截圖框線與箭頭標示。
- 旁白稿、字幕時間軸與 TTS。
- AI 周邊素材生成介面。
